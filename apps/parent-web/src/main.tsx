import { StrictMode, useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { bootstrapSession, googleLoginUrl, logout, type Session } from './auth-session';
import './styles.css';
export function ParentShell() { const [session, setSession] = useState<Session>(); const [resolved, setResolved] = useState(false); useEffect(() => { const controller = new AbortController(); let current = true; void bootstrapSession(() => setSession(undefined), controller.signal).then((next) => { if (current) { setSession(next); setResolved(true); } }); return () => { current = false; controller.abort(); }; }, []); if (!resolved) return <main><h1>PassionEdu</h1><p>Đang xác thực phiên...</p></main>; if (!session) return <main><h1>PassionEdu</h1><p>Vui lòng đăng nhập để tiếp tục.</p><a href={googleLoginUrl}>Đăng nhập với Google</a></main>; const contexts = session.schools; if (contexts.length === 1) return <main><h1>PassionEdu</h1><p>{contexts[0]!.schoolName}</p><p>Quyền xem của bạn đang được xác nhận theo từng học sinh.</p><button onClick={() => void logout(() => setSession(undefined))}>Đăng xuất</button></main>; return <main><h1>Chọn trường và học sinh</h1><p>Chỉ các liên kết đang hiệu lực được hiển thị.</p><ul>{contexts.map((context) => <li key={`${context.schoolId}-${context.student.id}`}>{context.schoolName}: {context.student.fullName}</li>)}</ul><button onClick={() => void logout(() => setSession(undefined))}>Đăng xuất</button></main>; }

const root = document.getElementById('root');
if (root) createRoot(root).render(<StrictMode><ParentShell /></StrictMode>);
