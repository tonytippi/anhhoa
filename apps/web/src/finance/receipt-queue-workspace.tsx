import { KeyboardEvent, useEffect, useRef, useState } from "react";
import type { FinanceStatus } from "./finance-workspace";

type Row = { id: string; student: { code: string; name: string }; class: { id: string; name: string }; schoolYearId: string; billingMonth: string; issuedAt: string; outstanding: string; status: "ISSUED" };
type Queue = { invoices: Row[]; filters: { schoolYearId: string | null; billingMonth: string; classIdSnapshot: string | null; student: string | null }; meta: { nextCursor: string | null } };
type Detail = { id: string; student: { code: string; name: string }; outstanding: string; status: "ISSUED" };
type Closed = { id: string; student: { code: string; name: string }; receipt: { actualAmount: string; outcome: "EXACT" | "SHORTFALL" | "OVERPAYMENT"; difference: { signedAmount: string } | null } | null; carries: Array<{ type: "SHORTFALL_CARRY" | "OVERPAYMENT_CARRY"; amount: string }>; coverageFacts?: Array<{ billingMonth: string; issuedAt: string | null }> };
type Operation = { status: string; outcome?: Closed };
const apiUrl = typeof __API_URL__ === "undefined" ? "" : __API_URL__;
const pendingKey = "passionedu.app.pending-receipt-queue-operation";
const csrf = () => document.cookie.split("; ").find((item) => item.startsWith("app_csrf="))?.slice("app_csrf=".length) ?? "";
const vnd = (value: string) => new Intl.NumberFormat("vi-VN").format(BigInt(value));
const outcome = (value: NonNullable<Closed["receipt"]>["outcome"]) => value === "EXACT" ? "Đủ" : value === "SHORTFALL" ? "Thu thiếu" : "Thu thừa";
const errorMessage = async (response: Response) => { try { const body = (await response.json()) as { error?: { message?: string; fieldErrors?: Record<string, string> } }; return body.error?.message ?? Object.values(body.error?.fieldErrors ?? {})[0] ?? "Không thể ghi thực nhận."; } catch { return "Không thể ghi thực nhận."; } };

export function ReceiptQueueWorkspace({ schoolId, schoolName, denied, onStatusChange }: { schoolId: string; schoolName: string; denied: () => void; onStatusChange?: (status: FinanceStatus) => void }) {
  const [queue, setQueue] = useState<Queue>(); const [filters, setFilters] = useState({ schoolYearId: "", billingMonth: "", classIdSnapshot: "", student: "" }); const [classes, setClasses] = useState<Array<{ id: string; name: string }>>([]); const [menu, setMenu] = useState<string>(); const [detail, setDetail] = useState<Detail>(); const [actual, setActual] = useState(""); const [pending, setPending] = useState<string>(); const [result, setResult] = useState<Closed>(); const [next, setNext] = useState<Row>(); const [message, setMessage] = useState("");
  const active = useRef(schoolId); const generation = useRef(0); const menuRef = useRef<HTMLDivElement>(null); const dialogRef = useRef<HTMLDivElement>(null); const trigger = useRef<HTMLButtonElement>(null);
  const query = (cursor?: string | null) => { const value = new URLSearchParams({ limit: "25" }); for (const [key, item] of Object.entries(filters)) if (item) value.set(key, item); if (cursor) value.set("cursor", cursor); return value; };
  const load = async (cursor?: string | null, replace = true, expected = generation.current) => { const response = await fetch(`${apiUrl}/api/app/schools/${schoolId}/finance/receipt-queue?${query(cursor)}`, { credentials: "include" }); if ([401, 403].includes(response.status)) { denied(); return; } if (!response.ok) throw new Error("Không thể tải hàng đợi thu tiền."); const value = ((await response.json()) as { data: Queue }).data; if (active.current !== schoolId || expected !== generation.current) return; setQueue((current) => replace ? value : { ...value, invoices: [...(current?.invoices ?? []), ...value.invoices] }); setFilters((current) => ({ ...current, schoolYearId: value.filters.schoolYearId ?? "", billingMonth: value.filters.billingMonth, classIdSnapshot: value.filters.classIdSnapshot ?? "", student: value.filters.student ?? "" })); return value; };
  const loadClasses = async (expected = generation.current) => { const value = new URLSearchParams(); if (filters.schoolYearId) value.set("schoolYearId", filters.schoolYearId); if (filters.billingMonth) value.set("billingMonth", filters.billingMonth); const response = await fetch(`${apiUrl}/api/app/schools/${schoolId}/finance/receipt-queue/classes?${value}`, { credentials: "include" }); if ([401, 403].includes(response.status)) { denied(); return; } if (response.ok && active.current === schoolId && expected === generation.current) setClasses(((await response.json()) as { data: { classes: Array<{ id: string; name: string }> } }).data.classes); };
  const clearProtected = () => { generation.current += 1; setMenu(undefined); setDetail(undefined); setResult(undefined); setNext(undefined); };
  const finished = async (closed: Closed, expected: number) => { if (expected !== generation.current) return; setDetail(undefined); setResult(closed); try { const refreshed = await load(undefined, true, expected); if (expected === generation.current) setNext(refreshed?.invoices.find((row) => row.id !== closed.id)); } catch { if (expected === generation.current) { setNext(undefined); setMessage("Đã ghi thực nhận; chưa thể tải lại hàng đợi mới nhất."); } } };
  const reconcile = async (operationId: string, expected = generation.current) => { try { const response = await fetch(`${apiUrl}/api/app/schools/${schoolId}/finance/operations/${operationId}`, { credentials: "include" }); if ([401, 403].includes(response.status)) { sessionStorage.removeItem(pendingKey); setPending(undefined); denied(); return; } if (!response.ok) throw new Error(); const operation = ((await response.json()) as { data: Operation }).data; if (expected !== generation.current || active.current !== schoolId) return; if (operation.status === "PENDING") { window.setTimeout(() => void reconcile(operationId, expected), 750); return; } sessionStorage.removeItem(pendingKey); setPending(undefined); if (operation.status === "COMPLETED" && operation.outcome) await finished(operation.outcome, expected); else setMessage("Thao tác không thành công."); } catch { if (expected === generation.current) { setPending(operationId); sessionStorage.setItem(pendingKey, JSON.stringify({ schoolId, id: operationId })); setMessage("Đang kiểm tra kết quả với hệ thống"); window.setTimeout(() => void reconcile(operationId, expected), 750); } } };
  useEffect(() => { active.current = schoolId; clearProtected(); setQueue(undefined); setClasses([]); setMessage(""); const expected = generation.current; void load(undefined, true, expected).then(() => loadClasses(expected)).catch((error: Error) => setMessage(error.message)); const saved = sessionStorage.getItem(pendingKey); if (saved) try { const value = JSON.parse(saved); if (value.schoolId === schoolId && typeof value.id === "string") { setPending(value.id); void reconcile(value.id, expected); } } catch { sessionStorage.removeItem(pendingKey); } }, [schoolId]);
  useEffect(() => { onStatusChange?.({ dirty: Boolean(detail), pending: Boolean(pending), reconcile: pending ? () => void reconcile(pending) : undefined }); }, [detail, pending]);
  useEffect(() => { if (menu) menuRef.current?.querySelector<HTMLButtonElement>("[role=menuitem]")?.focus(); }, [menu]);
  useEffect(() => { if (detail) dialogRef.current?.querySelector<HTMLInputElement>("input")?.focus(); else trigger.current?.focus(); }, [detail]);
  const applyFilters = () => { clearProtected(); const expected = generation.current; void Promise.all([load(undefined, true, expected), loadClasses(expected)]).catch((error: Error) => expected === generation.current && setMessage(error.message)); };
  const open = async (row: Row) => { clearProtected(); const expected = generation.current; const response = await fetch(`${apiUrl}/api/app/schools/${schoolId}/finance/receipt-queue/${row.id}`, { credentials: "include" }); if ([401, 403, 404].includes(response.status)) { if (expected === generation.current && response.status !== 404) denied(); else void load(undefined, true, expected).catch(() => {}); return; } if (!response.ok) { setMessage("Không thể mở hóa đơn đã chọn."); return; } const value = ((await response.json()) as { data: Detail }).data; if (expected !== generation.current || value.status !== "ISSUED") return; setDetail(value); setActual(value.outstanding); };
  const close = async () => { if (!detail || pending) return; const id = crypto.randomUUID(), expected = generation.current; setPending(id); sessionStorage.setItem(pendingKey, JSON.stringify({ schoolId, id })); try { const response = await fetch(`${apiUrl}/api/app/schools/${schoolId}/finance/invoices/${detail.id}/receipt`, { method: "POST", credentials: "include", headers: { "content-type": "application/json", "x-csrf-token": decodeURIComponent(csrf()), "idempotency-key": crypto.randomUUID(), "x-operation-id": id }, body: JSON.stringify({ actualAmount: actual }) }); if ([401, 403].includes(response.status)) { sessionStorage.removeItem(pendingKey); setPending(undefined); denied(); return; } if ([408, 502, 503, 504].includes(response.status)) { void reconcile(id, expected); return; } if (!response.ok) { sessionStorage.removeItem(pendingKey); setPending(undefined); setMessage(await errorMessage(response)); return; } const operation = ((await response.json()) as { data: Operation }).data; if (operation.status === "PENDING") { void reconcile(id, expected); return; } sessionStorage.removeItem(pendingKey); setPending(undefined); if (operation.status === "COMPLETED" && operation.outcome) await finished(operation.outcome, expected); else setMessage("Thao tác không thành công."); } catch { void reconcile(id, expected); } };
  const trap = (event: KeyboardEvent<HTMLDivElement>) => { if (event.key === "Escape" && !pending) { event.preventDefault(); setDetail(undefined); return; } if (event.key !== "Tab") return; const items = [...event.currentTarget.querySelectorAll<HTMLElement>("input, button")].filter((item) => !item.hasAttribute("disabled")); const first = items[0], last = items.at(-1); if (!first || !last) return; if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); } if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); } };
  return <section aria-labelledby="receipt-queue-title"><h2 id="receipt-queue-title">Thu tiền</h2><p>{schoolName}</p>{message && <p role="alert">{message}</p>}<form onSubmit={(event) => { event.preventDefault(); applyFilters(); }}><label>Năm học<input value={filters.schoolYearId} onChange={(event) => setFilters({ ...filters, schoolYearId: event.target.value })} /></label><label>Tháng thu<input type="month" value={filters.billingMonth} onChange={(event) => setFilters({ ...filters, billingMonth: event.target.value })} /></label><label>Lớp<select value={filters.classIdSnapshot} onChange={(event) => setFilters({ ...filters, classIdSnapshot: event.target.value })}><option value="">Tất cả lớp</option>{classes.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label>Mã hoặc tên học sinh<input value={filters.student} onChange={(event) => setFilters({ ...filters, student: event.target.value })} /></label><button>Lọc</button></form><table><caption>Hóa đơn chờ thu</caption><thead><tr><th>Học sinh</th><th>Lớp</th><th>Tháng</th><th>Còn phải thu</th><th>Trạng thái</th><th>Tùy chọn</th></tr></thead><tbody>{queue?.invoices.length ? queue.invoices.map((row) => <tr key={row.id}><td>{row.student.code} / {row.student.name}</td><td>{row.class.name}</td><td>{row.billingMonth}</td><td>{vnd(row.outstanding)} VND</td><td>Chờ thu</td><td><button ref={menu === row.id ? trigger : undefined} type="button" disabled={Boolean(pending)} aria-label={`Tùy chọn cho ${row.student.name}`} aria-haspopup="menu" aria-expanded={menu === row.id} onKeyDown={(event) => { if (["ArrowDown", "Enter", " "].includes(event.key)) { event.preventDefault(); setMenu(row.id); } if (event.key === "Escape") setMenu(undefined); }} onClick={() => setMenu(menu === row.id ? undefined : row.id)}>...</button>{menu === row.id && <div ref={menuRef} role="menu" onBlur={(event) => { if (!event.currentTarget.contains(event.relatedTarget as Node)) setMenu(undefined); }} onKeyDown={(event) => { const items = [...event.currentTarget.querySelectorAll<HTMLButtonElement>("[role=menuitem]")]; const index = items.indexOf(document.activeElement as HTMLButtonElement); if (["ArrowDown", "ArrowUp", "Home", "End"].includes(event.key)) { event.preventDefault(); items[event.key === "Home" ? 0 : event.key === "End" ? items.length - 1 : (index + (event.key === "ArrowDown" ? 1 : -1) + items.length) % items.length]?.focus(); } if (event.key === "Escape") { setMenu(undefined); trigger.current?.focus(); } }}><button role="menuitem" type="button" onClick={() => void open(row)}>Ghi thực nhận</button></div>}</td></tr>) : <tr><td colSpan={6}>Không có hóa đơn chờ thu.</td></tr>}</tbody></table>{detail && <div ref={dialogRef} role="dialog" aria-modal="true" aria-labelledby="receipt-title" onKeyDown={trap}><h3 id="receipt-title">Ghi thực nhận cho {detail.student.name}</h3><p>Nghĩa vụ do máy chủ xác nhận: {vnd(detail.outstanding)} VND. Kết quả, chênh lệch, chuyển kỳ và coverage do máy chủ xác định.</p><label>Số thực nhận (VND)<input inputMode="numeric" value={actual} onChange={(event) => setActual(event.target.value)} /></label><button type="button" disabled={Boolean(pending) || !actual} onClick={() => void close()}>Xác nhận ghi thực nhận</button><button type="button" disabled={Boolean(pending)} onClick={() => setDetail(undefined)}>Hủy</button></div>}{result && result.receipt && <section aria-labelledby="receipt-result-title"><h3 id="receipt-result-title">Kết quả ghi thực nhận</h3><p>Thực nhận: {vnd(result.receipt.actualAmount)} VND.</p><p>Kết quả máy chủ: {outcome(result.receipt.outcome)}.</p><p>Chênh lệch: {vnd(result.receipt.difference?.signedAmount ?? "0")} VND.</p><p>{(result.carries ?? []).length ? (result.carries ?? []).map((carry) => `${carry.type === "SHORTFALL_CARRY" ? "Khoản thu thiếu chuyển kỳ" : "Khoản thu thừa khấu trừ kỳ sau"}: ${vnd(carry.amount)} VND.`).join(" ") : "Trạng thái chuyển kỳ: Máy chủ chưa tạo khoản chuyển kỳ cho hóa đơn này."}</p><p>{(result.coverageFacts ?? []).length ? (result.coverageFacts ?? []).map((fact) => fact.issuedAt ? `Coverage kỳ ${fact.billingMonth} đã phát hành.` : `Coverage kỳ ${fact.billingMonth} chưa phát hành.`).join(" ") : "Kết quả coverage: Không có coverage áp dụng."}</p>{next && <button type="button" onClick={() => void open(next)}>Hóa đơn tiếp theo</button>}</section>}</section>;
}
