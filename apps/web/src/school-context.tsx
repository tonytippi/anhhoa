import { FormEvent, useEffect, useLayoutEffect, useRef, useState } from 'react';
import { RosterWorkspace } from './roster/roster-workspace';

type School = { schoolId: string; schoolName: string };
type Role = 'SCHOOL_ADMIN' | 'FINANCE_MANAGER' | 'CLASS_TEACHER';
type Context = { schoolId: string; schoolName: string; membershipId: string; capabilities: Array<'SCHOOL_CONTEXT_READ' | 'ACCESS_MANAGE' | 'ROSTER_MANAGE'>; navigation: Array<{ id: string; label: string }> };
type Membership = { id: string; email: string; status: 'ACTIVE' | 'REVOKED'; roles: Role[] };
type Pending = { id: string; schoolId: string };
type RosterStatus = { dirty: boolean; pending: boolean; reconcile?: () => void };
const apiUrl = typeof __API_URL__ === 'undefined' ? '' : __API_URL__;
const csrfName = typeof __CSRF_COOKIE_NAME__ === 'undefined' ? 'app_csrf' : __CSRF_COOKIE_NAME__;
const pendingKey = 'passionedu.app.pending-membership-operation';
const presets: Role[] = ['SCHOOL_ADMIN', 'FINANCE_MANAGER', 'CLASS_TEACHER'];
const csrf = () => document.cookie.split('; ').find((item) => item.startsWith(`${csrfName}=`))?.slice(csrfName.length + 1);
const denied = (status: number) => [401, 403, 404].includes(status);
const uncertain = (status: number) => [408, 502, 503, 504].includes(status);

export function SchoolContext({ clear }: { clear: () => void }) {
  const [schools, setSchools] = useState<School[]>(); const [context, setContext] = useState<Context>(); const [memberships, setMemberships] = useState<Membership[]>([]); const [email, setEmail] = useState(''); const [roles, setRoles] = useState<Role[]>(['CLASS_TEACHER']); const [dirty, setDirty] = useState(false); const [pending, setPending] = useState<Pending>(); const [roster, setRoster] = useState(false); const [rosterDirty, setRosterDirty] = useState(false); const [rosterPending, setRosterPending] = useState(false); const [switchTo, setSwitchTo] = useState<string>(); const [confirm, setConfirm] = useState<{ membershipId: string; roles?: Role[]; action: 'revoke' | 'replace' }>(); const [reason, setReason] = useState(''); const [reasonError, setReasonError] = useState(''); const [error, setError] = useState('');
  const mounted = useRef(true); const selected = useRef<string | undefined>(undefined); const version = useRef(0); const timer = useRef<number | undefined>(undefined); const heading = useRef<HTMLHeadingElement>(null); const dialog = useRef<HTMLDivElement>(null); const rosterReconcile = useRef<(() => void) | undefined>(undefined);
  const current = (schoolId: string, requestVersion?: number) => mounted.current && selected.current === schoolId && (requestVersion === undefined || version.current === requestVersion);
  const stop = () => { if (timer.current) window.clearTimeout(timer.current); timer.current = undefined; };
  const clearContext = () => { version.current += 1; selected.current = undefined; rosterReconcile.current = undefined; stop(); sessionStorage.removeItem(pendingKey); setContext(undefined); setMemberships([]); setPending(undefined); setDirty(false); setRosterDirty(false); setRosterPending(false); setRoster(false); setSwitchTo(undefined); setConfirm(undefined); };
  const refreshChooser = async () => { const response = await fetch(`${apiUrl}/api/app/schools`, { credentials: 'include' }); if (response.status === 401) { clearContext(); clear(); return; } if (!response.ok) throw new Error('Không thể tải danh sách trường.'); if (mounted.current) setSchools((await response.json() as { data: School[] }).data); };
  const loadMemberships = async (schoolId: string, requestVersion = version.current) => { const response = await fetch(`${apiUrl}/api/app/schools/${schoolId}/memberships`, { credentials: 'include' }); if (!current(schoolId, requestVersion)) return; if (denied(response.status)) { clearContext(); return refreshChooser(); } if (!response.ok) throw new Error('Không thể tải danh sách truy cập.'); const data = (await response.json() as { data: Membership[] }).data; if (current(schoolId, requestVersion)) setMemberships(data); };
  const load = async (schoolId: string) => { const requestVersion = ++version.current; selected.current = schoolId; rosterReconcile.current = undefined; stop(); setContext(undefined); setMemberships([]); setRoster(false); setRosterDirty(false); setRosterPending(false); const response = await fetch(`${apiUrl}/api/app/schools/${schoolId}`, { credentials: 'include' }); if (!current(schoolId, requestVersion)) return; if (denied(response.status)) { clearContext(); return refreshChooser(); } if (!response.ok) throw new Error('Không thể tải ngữ cảnh trường.'); const next = (await response.json() as { data: Context }).data; if (!current(schoolId, requestVersion)) return; setContext(next); if (next.capabilities.includes('ACCESS_MANAGE')) await loadMemberships(schoolId, requestVersion); };
  const reconcile = async (operation: Pending) => { if (!current(operation.schoolId)) return; setPending(operation); try { const response = await fetch(`${apiUrl}/api/app/schools/${operation.schoolId}/operations/${operation.id}`, { credentials: 'include' }); if (!current(operation.schoolId)) return; if (denied(response.status)) { clearContext(); return refreshChooser(); } if (!response.ok) throw new Error(); const result = (await response.json() as { data: { status: string } }).data; if (!current(operation.schoolId)) return; if (result.status === 'PENDING') { timer.current = window.setTimeout(() => void reconcile(operation), 750); return; } sessionStorage.removeItem(pendingKey); setPending(undefined); if (result.status === 'COMPLETED') { await loadMemberships(operation.schoolId); if (current(operation.schoolId)) { setEmail(''); setRoles(['CLASS_TEACHER']); setDirty(false); } } else setError('Thao tác không thành công.'); } catch { if (current(operation.schoolId)) timer.current = window.setTimeout(() => void reconcile(operation), 750); } };
  useEffect(() => { mounted.current = true; void refreshChooser().then(async () => { const raw = sessionStorage.getItem(pendingKey); if (!raw) return; const operation = JSON.parse(raw) as Pending; await load(operation.schoolId); if (current(operation.schoolId)) void reconcile(operation); }).catch(() => { clearContext(); clear(); }); return () => { mounted.current = false; stop(); }; }, []);
  useLayoutEffect(() => { heading.current?.focus(); }, [context?.schoolId]);
  useEffect(() => { const revalidate = () => { if (document.visibilityState === 'visible' && selected.current) void load(selected.current).catch((cause: Error) => setError(cause.message)); }; document.addEventListener('visibilitychange', revalidate); window.addEventListener('focus', revalidate); return () => { document.removeEventListener('visibilitychange', revalidate); window.removeEventListener('focus', revalidate); }; }, []);
  useEffect(() => { if (switchTo || confirm) dialog.current?.querySelector<HTMLElement>('input, button, textarea')?.focus(); }, [switchTo, confirm]);
  const requestSwitch = (schoolId: string) => { if (schoolId === context?.schoolId) return; if (pending || dirty || rosterDirty || rosterPending) setSwitchTo(schoolId); else void load(schoolId).catch((cause: Error) => setError(cause.message)); };
  const mutate = async (path: string, body: object) => { const schoolId = selected.current; if (!schoolId || pending) return false; const operation = { id: crypto.randomUUID(), schoolId }; try { const response = await fetch(`${apiUrl}${path}`, { method: 'POST', credentials: 'include', headers: { 'content-type': 'application/json', 'x-csrf-token': decodeURIComponent(csrf() ?? ''), 'idempotency-key': crypto.randomUUID(), 'x-operation-id': operation.id }, body: JSON.stringify(body) }); if (!current(schoolId)) return false; if (denied(response.status)) { clearContext(); await refreshChooser(); return false; } if (uncertain(response.status)) throw new TypeError('Mutation outcome is uncertain.'); if (!response.ok) throw new Error((await response.json() as { error?: { message?: string } }).error?.message ?? 'Thao tác không thành công.'); await loadMemberships(schoolId); return current(schoolId); } catch (cause) { if (!current(schoolId)) return false; if (cause instanceof TypeError) { sessionStorage.setItem(pendingKey, JSON.stringify(operation)); setPending(operation); void reconcile(operation); } else setError(cause instanceof Error ? cause.message : 'Thao tác không thành công.'); return false; } };
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!email || !roles.length) return setError('Chọn ít nhất một preset.'); if (await mutate(`/api/app/schools/${context!.schoolId}/memberships`, { email, roles })) { setEmail(''); setRoles(['CLASS_TEACHER']); setDirty(false); } };
  const confirmAction = async () => { if (!confirm) return; if (!reason.trim()) return setReasonError('Cần nêu lý do.'); const path = confirm.action === 'revoke' ? `/api/app/schools/${context!.schoolId}/memberships/${confirm.membershipId}/revoke` : `/api/app/schools/${context!.schoolId}/memberships/${confirm.membershipId}/roles`; const body = confirm.action === 'revoke' ? { reason: reason.trim() } : { roles: confirm.roles, reason: reason.trim() }; setConfirm(undefined); setReason(''); setReasonError(''); await mutate(path, body); };
  if (!schools) return <p>Đang tải ngữ cảnh trường...</p>;
  if (!schools.length) return <p>Không có trường nào đang cấp quyền cho tài khoản này.</p>;
  const switchingBlocked = pending || rosterPending;
  return <section className="school-context"><label>Chọn trường <select aria-label="Chọn trường" value={context?.schoolId ?? ''} disabled={Boolean(switchingBlocked)} onChange={(event) => requestSwitch(event.target.value)}><option value="" disabled>Chọn trường</option>{schools.map((school) => <option key={school.schoolId} value={school.schoolId}>{school.schoolName}</option>)}</select></label>{error && <p role="alert">{error}</p>}{context && <><h1 ref={heading} tabIndex={-1}>PassionEdu - {context.schoolName}</h1><nav aria-label="Điều hướng trường">{context.navigation.map((item) => item.id === 'roster' && context.capabilities.includes('ROSTER_MANAGE') ? <button key={item.id} aria-current={roster ? 'page' : undefined} onClick={() => setRoster(true)}>{item.label}</button> : <span key={item.id}>{item.label} </span>)}{context.capabilities.includes('ROSTER_MANAGE') && !context.navigation.some((item) => item.id === 'roster') && <button onClick={() => setRoster(true)}>Danh bộ</button>}</nav>{roster ? <><button onClick={() => setRoster(false)}>Quản lý truy cập</button><RosterWorkspace schoolId={context.schoolId} schoolName={context.schoolName} denied={() => { clearContext(); void refreshChooser(); }} onStatusChange={(status: RosterStatus) => { rosterReconcile.current = status.reconcile; setRosterDirty(status.dirty); setRosterPending(status.pending); }} /></> : context.capabilities.includes('ACCESS_MANAGE') && <section aria-labelledby="access-title"><h2 id="access-title">Quản lý truy cập</h2><form onSubmit={submit}><label>Email<input required type="email" value={email} onChange={(event) => { setEmail(event.target.value); setDirty(true); }} /></label><fieldset><legend>Preset truy cập</legend>{presets.map((role) => <label key={role}><input type="checkbox" checked={roles.includes(role)} onChange={() => { setRoles((value) => value.includes(role) ? value.filter((item) => item !== role) : [...value, role]); setDirty(true); }} />{role}</label>)}</fieldset><button disabled={Boolean(pending)}>{pending ? 'Đang đối soát...' : 'Cấp quyền'}</button></form><table><caption>Membership của {context.schoolName}</caption><thead><tr><th scope="col">Email</th><th scope="col">Preset</th><th scope="col">Trạng thái</th><th scope="col">Thao tác</th></tr></thead><tbody>{memberships.length ? memberships.map((member) => <tr key={member.id}><td>{member.email}</td><td>{member.roles.join(', ') || 'Không có'}</td><td>{member.status}</td><td>{member.status === 'ACTIVE' && <><button disabled={Boolean(pending)} onClick={() => { setRoles(member.roles); setConfirm({ membershipId: member.id, roles: member.roles, action: 'replace' }); }}>Thay preset</button><button disabled={Boolean(pending)} onClick={() => setConfirm({ membershipId: member.id, action: 'revoke' })}>Thu hồi</button></>}</td></tr>) : <tr><td colSpan={4}>Chưa có membership nào.</td></tr>}</tbody></table></section>}</>}{(switchTo || confirm) && <><div className="dialog-backdrop" /><div ref={dialog} className="dialog" role="dialog" aria-modal="true" aria-labelledby="membership-dialog-title">{switchTo ? <><h2 id="membership-dialog-title">Đổi trường?</h2>{switchingBlocked ? <><p>Thao tác trước đó chưa chắc kết quả. Hãy đối soát trước khi đổi trường.</p><button onClick={() => pending ? void reconcile(pending) : rosterReconcile.current?.()}>Đối soát thao tác</button></> : <><p>Biểu mẫu đang có nội dung chưa gửi. Dữ liệu sẽ không được tự lưu.</p><button onClick={() => setSwitchTo(undefined)}>Ở lại</button><button onClick={() => { const next = switchTo; setEmail(''); setRoles(['CLASS_TEACHER']); setDirty(false); setRosterDirty(false); setSwitchTo(undefined); void load(next); }}>Bỏ nội dung và đổi trường</button></>}</> : <><h2 id="membership-dialog-title">{confirm!.action === 'revoke' ? 'Xác nhận thu hồi quyền' : 'Xác nhận thay preset'}</h2><label>Lý do<textarea autoFocus value={reason} onChange={(event) => { setReason(event.target.value); setReasonError(''); }} /></label>{reasonError && <p role="alert">{reasonError}</p>}<button onClick={() => setConfirm(undefined)}>Hủy</button><button onClick={() => void confirmAction()}>Xác nhận</button></>}</div></>}</section>;
}
