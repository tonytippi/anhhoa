---
epic: 4
date: 08-20-2026
verdict: rejected
criteria: declared
headless: false
---

# Retrospective Epic 4: Đối soát thu tiền theo tháng

## Epic summary

Epic 4 đã hoàn thành Story 4.1: báo cáo tháng read-only và tổng quan lấy từ completed Invoice snapshot. `sprint-status.yaml:62-64` ghi nhận story `done`; `detect-epic --epic 4` không trả về story dang dở.

Phạm vi đã kiểm tra là `c517bb1..c0bc9d4`, gồm hai commit không merge và 22 file thay đổi. Bằng chứng gồm Epic context, Story 4.1 specification, toàn bộ git range, report và invoice-detail source hiện tại, sprint status, unit/component test, PostgreSQL integration test và Playwright E2E test. Không có session log. Action item retrospective Epic 3 đã `done` trong `sprint-status.yaml:65-107`; không có item mở cũ cần follow-through.

## Findings

### Kiểm tra hành vi xuyên Epic

1. **Hoàn tất hóa đơn làm dữ liệu report và overview đã cache bị stale.**
   - Nguồn: `apps/web/src/features/invoices/detail-page.tsx:71`, `apps/web/src/features/reports/api.ts:17`, `apps/web/src/features/overview/page.tsx:8-12`.
   - Điều kiện: Admin đã tải report hoặc overview của tháng hóa đơn, hoàn tất hóa đơn `PENDING`, rồi quay lại tháng đã cache trước khi có refetch ngẫu nhiên.
   - Hệ quả: tổng đã thu, tổng tiền mặt/chuyển khoản, completed count và breakdown tài khoản nhận tiền có thể hiển thị số liệu trước completion.
   - Hướng xử lý: **sửa ngay**. Trong `finishCompletion`, invalidate `['reports', 'monthly', result.billingMonth]` cùng invoice-list invalidation, và thêm query-client/UI regression test hoàn tất hóa đơn sau khi report month đã cache. Phòng ngừa từ gốc là checklist invalidation từ mutation đến mọi derived resource aggregate.

### Aggregate views

- Architecture delta: module `reports` là read-only, controller-to-service layering được giữ và report parsing vẫn ở web REST boundary. Service aggregate completed invoice từ snapshot, phù hợp `epic-4-context.md:15-29`.
- Duplication và size: Epic thêm các report module và web utility nhỏ, chuyên trách; không phát hiện god-class growth hoặc report aggregation trùng lặp trong phạm vi kiểm tra.
- Spec reconciliation: Story 4.1 thiết lập đúng completed-only snapshot aggregation và responsive report surface, nhưng completion flow của Epic 3 không refresh derived resource mới này. Đây là integration defect, không phải scope deviation được chấp nhận.

## Xác minh hành vi

- `pnpm lint`, `pnpm typecheck`, `pnpm test` và `pnpm build` đều pass. Toàn bộ unit/component run pass 93 API test và 88 web test.
- `pnpm --filter api test:integration` pass 35 PostgreSQL-backed test sau fresh migration. Process phát sinh cảnh báo deprecation `pg` về concurrent query khi teardown.
- `pnpm --filter web test:e2e` pass 25 Playwright test, gồm monthly report, overview shortcut, snapshot breakdown và mobile report behavior.
- Report flow và completion flow được test riêng; chưa có test nào chạy cached monthly report rồi completion, tức điều kiện xuyên Epic chưa được cover ở trên.

## Follow-through retrospective trước

Action item Epic 3 `epic-3-retro-item-3-sửa-batch-và-completion-reconciliation-đ`, `epic-3-retro-item-4-thêm-postgresql-integration-test-cho-inv` và `epic-3-retro-item-5-siết-parseinvoicedetail-yêu-cầu-complete` đã `done` trong `sprint-status.yaml:82-106`. Chúng nằm ngoài phạm vi follow-through item mở của retrospective này.

## Action items

1. **Đề xuất, owner: Amelia.** Invalidate monthly report query bị ảnh hưởng sau invoice completion thành công và thêm regression test chứng minh total, count và transfer breakdown refresh khi report month đã cache nhận hóa đơn mới completed.

## Kết luận acceptance

**Rejected.** Criteria được công bố trong Story 4.1. Report tự aggregate và render đúng snapshot data yêu cầu, direct test cũng pass, nhưng có thể hiển thị collection total stale sau khi người dùng hoàn tất hóa đơn trong cùng session. Điều này trái với mục tiêu đối soát tháng đáng tin cậy của Epic. Không có human override.

## Câu hỏi mở

- Invoice completion có cần invalidate report month đã cache ở inactive tab qua shared stale-time policy không, hay invalidate QueryClient hiện tại là đủ cho MVP?

## Resolution addendum

- **2026-08-20:** Action item `epic-4-retro-item-7-invalidate-the-affected-monthly-report-q` đã hoàn thành qua `spec-fix-epic-1-4-auth-site-and-report-cache.md`. Terminal completion giờ invalidate exact monthly report key từ server `billingMonth`; Playwright cache hai tháng fresh trong cùng SPA session và xác minh chỉ tháng hoàn tất refetch count, total và transfer snapshot breakdown.
